llm:
  provider: ollama           # "openai" or "ollama"
  model: qwen3:32b         # LLM model name

embeddings:
  provider: ollama           # "openai" or "ollama"
  model: nomic-ai/nomic-embed-text-v2-moe

neo4j:
  uri: bolt://localhost:7687
  user: neo4j
  password: 55555555
  database: propresyn-gba-seed

pdf:
  input_dir: ./tests/pdfs          # folder containing PDF files named by PMID

pipeline:
  similarity_threshold: 0.85 # cosine similarity threshold for entity matching